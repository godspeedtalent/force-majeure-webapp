/**
 * Centralized Query Utilities
 *
 * Export all React Query hooks and utilities for type-safe, cached data fetching.
 *
 * Usage:
 * ```ts
 * import { useEventById, useEvents, useCreateEvent } from '@/shared/api/queries';
 * ```
 */

export * from './eventQueries';

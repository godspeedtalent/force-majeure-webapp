/**
 * Layout configuration for Tailwind CSS
 * Container and responsive design settings
 */
export const container = {
    center: true,
    padding: '2rem',
    screens: {
        '2xl': '1400px',
    },
};
